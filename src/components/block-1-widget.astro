---
const { 
  title = "INTRODUCING", 
  description = `Utya isn’t just a meme coin , it’s the soul of Telegram’s sticker culture!  
Born from the mind of legendary designer Egor Zhgun in 2020, this yellow duck has flown beyond memes and into the world of NFTs.  
Want proof? Check out the original art <a href="https://www.behance.net/gallery/93658133/Utya-Duck-Telegram-Stickers?locale=en_US" target="_blank" rel="noopener noreferrer" style="color: #ffd527;">here on Behance</a> , and dive into <a href="https://t.me/addstickers/UtyaDuck" target="_blank" rel="noopener noreferrer" style="color: #ffd527;">Utya’s sticker</a> world  
right on <a href="https://telegram.org/" target="_blank" rel="noopener noreferrer" style="color: #ffd527;">Telegram</a>.  
Today, Utya is more than just cute , it's a movement.
` } = Astro.props;
---

<style>
.block-1-widget {
  display: flex;
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.05),
    rgba(255, 255, 255, 0.05)
  );
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-radius: 16px;
  padding: 24px 48px; 
  margin: 24px auto;
  max-width: 1100px;
  min-height: 500px;
  color: white;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  font-family: 'Fredoka One', cursive, sans-serif;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.block-1-widget .description a {
  color: #ffd527;
  text-decoration: none;
}
.block-1-widget .description a:hover {
  opacity: 0.8;
}

.block-1-widget .content {
  flex: 1 1 50%;
  padding-right: 24px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.block-1-widget .intro-heading {
  margin-bottom: 16px;
}

.block-1-widget .intro-heading div {
  line-height: 1.2;
}

.block-1-widget .intro-intro {
  font-size: 1.2rem;
  color: rgba(141, 82, 193, 1);
}

.block-1-widget .intro-name {
  font-size: 3rem;
  color: rgba(255, 213, 39, 1);
  margin-top: 8px;
}

.block-1-widget .intro-tagline {
  font-size: 1.2rem;
  margin-top: 8px;
  color: rgba(141, 82, 193, 1);
}

.block-1-widget .description {
  font-size: 1.1rem;
  line-height: 1.6;
  margin-bottom: 24px;
}

.block-1-widget .description a {
  color: #ffd527;
  text-decoration: underline;
}

.block-1-widget .lottie-container {
  flex: 1 1 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border-radius: 16px;
}

.block-1-widget .lottie-container > div {
  width: 100%;
  max-width: 400px;
  max-height: 400px;
}
  
@media (max-width: 768px) {
  .block-1-widget {
    flex-direction: column;
    min-height: auto;
  }
  .block-1-widget .content,
  .block-1-widget .lottie-container {
    flex: 1 1 100%;
    padding-right: 0;
    margin-bottom: 24px;
  }
}
</style>

<div class="block-1-widget" role="region" aria-label="Intro Utya widget">
  <div class="content">
    <h2 class="intro-heading">
      <div class="intro-intro">{title}</div>
      <div class="intro-name">UTYA</div>
      <div class="intro-tagline">The Face of Telegram’s Meme Culture</div>
    </h2>
    <div class="description" set:html={description}></div>
  </div>
  <div class="lottie-container" id="lottie-container"></div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/lottie-web/5.10.2/lottie.min.js"></script>
<script type="module">
  const container = document.getElementById('lottie-container');
  let animation = null;

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        if (!animation) {
          animation = lottie.loadAnimation({
            container: container,
            renderer: 'svg',
            loop: true,
            autoplay: true,
            path: '/utya-haha-classic.json'
          });
        } else {
          animation.play();
        }
      } else {
        if (animation) animation.pause();
      }
    });
  }, { threshold: 0.3 });

  if(container) observer.observe(container);
</script>
